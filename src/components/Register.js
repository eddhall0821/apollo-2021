import { Form, Modal } from "antd";
import React from "react";

const RegisterModal = () => {
  return <Form></Form>;
};

const Register = () => {
  return;
  <RegisterModal />;
};

export default Register;
